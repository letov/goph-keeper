-- +goose Up
CREATE TABLE binaries (
		id SERIAL PRIMARY KEY,
		created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
		updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
		owner INTEGER NOT NULL,
		meta_key INTEGER DEFAULT NULL,
		binary_key INTEGER NOT NULL,
		CONSTRAINT "FK_binaries__owner___users__id" FOREIGN KEY ("owner") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE NO ACTION
);

-- +goose Down
DROP TABLE binaries;
