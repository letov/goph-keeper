-- +goose Up
CREATE TABLE login_passwords (
		id SERIAL PRIMARY KEY,
		created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
		updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
		owner INTEGER NOT NULL,
		meta_key INTEGER DEFAULT NULL,
		login_key INTEGER DEFAULT NULL,
		pass_key INTEGER DEFAULT NULL,
		CONSTRAINT "FK_login_passwords__owner___users__id" FOREIGN KEY ("owner") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE NO ACTION
);

-- +goose Down
DROP TABLE login_passwords;
